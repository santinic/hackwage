let postprocessing = {
    // This is now done in postproc, but for example:
    // "GitHub Remote": (doc) => {
    //     doc.body_html = '<pre>'+doc.body+'</pre>';
    // }
};

module.exports = postprocessing;